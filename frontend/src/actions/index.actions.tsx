import { getAll } from '../API/ReadableAPI'

export function handleInitialData () {
    // return (dispatch: any) => {
    //   return Promise.all([
    //       getAll()
    //   ])
    //   .then([posts: any, categories: any])
    // }

    // ReadbleAPI.getAll()
    // .then(categories => {
    //   // console.log('Categoriesss',categories)
    //    this.setState({categories: categories})
    // })
}